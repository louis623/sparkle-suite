# Sparkle Finder Project State

September 9 LOC integration: private evidence is live and verified. Migration `20260909123000` fixes missing server-role SELECT on the two intake tables; RLS and anonymous denial remain. Synthetic signed-image/read-denial tests pass and test data was removed. Production `6e9f708e` / `dpl_4RShdV6kTVsD9NjDuYiAFVsf1er9` is confirmed at `yoursparklefinder.com`. Full LOC queued-review mutation remains a separate gate.

## September 8, 2026 - LOC private review integration built locally

Added a separately authenticated private review-evidence endpoint for LOC's native Finder control-center review. Finder retains its own database, auth and deployment. 38 focused tests and production build pass; LOC fixture review/picker checks pass. No Finder deployment or secret configuration changed. Live synthetic evidence verification remains. See Suite vault `2026-09-08-loc-control-center-build-checkpoint.md`.

Sparkle Finder's active implementation workspace is `C:\Users\louis\sparkle-suite-repo\apps\finder` on the Suite repository's allowlisted `codex/nic-nac-trade-hardening` branch.

The former lightweight binder at `C:\Users\louis\sparkle-finder` has been folded into this repo for durable docs, plans, handoffs, vault memory, and repo-local skills. This keeps Sparkle Finder code and Open Brain/binder memory under one workspace root and avoids recurring Codex sandbox permission prompts.

Future Codex sessions should open `C:\Users\louis\sparkle-suite-repo` with Workspace write enabled, then work on Finder from `apps\finder`.

## Current Live Target

- Primary customer and production domain: `https://yoursparklefinder.com`
- Vercel project `sparkle-finder-dev` is connected to GitHub repository
  `louis623/sparkle-suite` with Root Directory `apps/finder` and production
  branch `codex/nic-nac-trade-hardening`.
- The stable production Vercel alias `https://sparkle-finder-dev.vercel.app` permanently redirects to the matching path and query on the customer domain. Preview deployments remain available for private verification.
- Production-style repo work should happen from this implementation repo, not the old binder.

## Recent Session Context

- August 25, 2026 Suite compatibility Releases 1-3 are complete and live. Sparkle Suite release `f3de6c15` is the canonical `schemaVersion: 2` source for designs, exact variants, reps, shows, pending-adjusted dancer availability, and Showcase Studio continuation. Finder's strict live gate passes against that production contract.
- Finder Release 1 (`0c54a32`) added stable catalog pagination, whole-result facets, exact batch hydration, and exact-`designId` variant preservation. Release 2 (`93107f6`) made Dance Floor results quantity-aware, separated rep leads from available dancers, excluded zero/removed inventory, and kept all availability calculation in Suite. Both releases were independently deployed and verified at `https://yoursparklefinder.com`.
- Finder Release 3 shipped in `b8ecf57` with smoke-test follow-up `0613800`. The active phone-first Showcase Studio uses the Suite v2 resolve/confirm/replay contract, sends persisted private photo evidence rather than base64 payloads, preserves exact candidate identities without auto-selection, and uses owner-verified service-role writes with monotonic recovery states. Finder-private migration `20260825143000_sparkle_finder_showcase_studio_recovery.sql` is applied and remote migration history is current. Deployment `dpl_34Hs61FVJmuLi4E6pMcX8zZCJSvc` is `READY` and custom-domain verified. Verification passed lint, production build, `56` Vitest files / `756` tests, the strict Suite contract gate, `20` required browser smoke checks with `2` expected optional skips, the Nic-Nac guard, and 320px/390px/tablet/desktop visual checks.
- Compatibility Release 4 is complete in `1b52f44` and deployed as `dpl_GKS4RzyHxnpchfYsypE3q3UT67DR`. Sanitized fixtures now cover exact variants, three-page catalog and availability continuation, positive/zero/removed dancers, and stable-submission Studio ambiguity/replay. The strict checker samples up to five pages with cursor-cycle, repeated-identity, total-drift, and terminal-aggregate guards. Final checks passed lint, build, `57` Vitest files / `760` tests, strict live Suite verification, `20` required browser smokes with `2` expected skips, Nic-Nac guard, custom-domain `200`, and empty recent runtime errors. Positive signed-in production Studio mutation/replay verification remains deferred until Louis designates a demo account and data; no arbitrary production customer data should be created for that check.
- August 22, 2026 launch-quality follow-up: The approved five-release Showcase hardening plan is complete. Public data is protected at the database and application layers; broken public routes and owned-only Rarest Reveal semantics are repaired; owner controls are phone-first; copy, empty states, and failure feedback are polished; and public reads are bounded, block-aware, cached per request, and backed by exact database summaries. Migrations `20260822210000_sparkle_finder_showcase_public_read_hardening.sql`, `20260822220000_sparkle_finder_owned_rarest_reveals.sql`, and `20260822230000_sparkle_finder_bounded_showcase_reads.sql` are applied and the remote migration history is current. Supabase compromised-password protection is enabled. Commit `885e26d` is deployed Ready as `dpl_5i3EPnnTDM2Yg1jFNZzqJ8oZLB34`; alternate production hosts now canonicalize to `https://yoursparklefinder.com`. Final verification passed all `52` test files / `640` tests, lint, production build, release smoke, and live domain checks.
- August 22, 2026: The five-part collection and social-showcase release is implemented. Customer collections now have a richer mobile-first showroom, durable single Hero Piece selection, owner-managed public/private Showcase profiles and Showcase Collections, reveal stories and personal photos, phone-friendly sharing with canonical metadata, and bounded Followed Showcases discovery. Public Showcase reads are now database-backed and fail closed around profile, Showcase, piece, collection, and block visibility; production fixture fallback is disabled. Migrations `20260822170000_sparkle_finder_single_hero_piece.sql` and `20260822193000_followed_showcase_highlights.sql` are applied, and `supabase db push --yes` reports the remote database is current. Commit `85f786b` is deployed Ready as `dpl_ZaPVexp3AWvtK75DuHLkdMvoNmLm` and aliased at `https://yoursparklefinder.com`.
- August 22, 2026: Approved customer and rep vocabulary is `Dance Floor` for the feature and `dancers` for its trade inventory. A `trade` remains a trade. Legacy route, database, API, field, component, and tool identifiers remain unchanged for compatibility, but must never leak into visible copy or Nic-Nac responses.
- August 21, 2026: The automatic Sparkle Suite-to-Finder Reps feed is live. Sparkle Suite now exposes a safe, service-backed `GET /api/public/finder/reps` route with explicit server-managed visibility, no demo/reviewer leakage, canonical customer-site/Dance Floor links, and safe next-show data. Heather/BlingKitchen is the sole initial public rep because she is the one confirmed real active beta rep; normal future real-rep provisioning opts in automatically, while reviewer/demo paths remain hidden. Finder's live contract checker reports `REPS=1`. Finder-owned aggregate favorite counts remain private, bounded, and database-backed, with the Free five-rep cap enforced by RLS.

- Sparkle Finder silver showcase workflow was simplified for phone-first use.
- Advanced showcase/profile controls were removed from the main customer flow in favor of simple cards, simple panels, and Nic-Nac-led help.
- Nic-Nac lead behavior should use customer-friendly wording, local/customer-facing show times, clear buttons for Dance Floor and show/calendar links, and 48-hour freshness/expiration rules.
- June 22, 2026: Finder `/api/finder/nic-nac` now uses the OpenAI-only Nic-Nac model policy adapter (`human_default`) instead of hardcoded Anthropic Haiku. The route keeps Silver gating and Finder-local tools/prompts, but model selection is no longer embedded in the route. Finder Nic-Nac prompts now receive linked-rep surface context when a Finder account is tied to a Sparkle Suite rep, so Nic-Nac can preserve same-assistant identity while telling reps that Sparkle Suite mutations require opening/logging into Sparkle Suite. The route also automatically preloads bounded safe Finder customer memory into the system prompt, while filtering unsafe memory before it reaches the model. Finder now applies a conservative Nic-Nac mission guard before OpenAI configuration, Supabase memory, tool setup, or model streaming, so clear off-mission requests receive a static redirect stream without burning model credits. Finder Vercel now has `OPENAI_API_KEY` and model env vars, and a deployed linked-rep Nic-Nac stream smoke passed earlier today.
- June 22, 2026 later: Finder Nic-Nac durable telemetry is implemented, deployed, and runtime-smoked. The route now attempts fail-open persistence of conversations, messages, and run telemetry for mission redirects and model-backed streams. The telemetry migration was applied manually through the Supabase Dashboard to the `sparkle-finder-auth` project and directly verified for tables, RLS, read-own policies, service-role grants, triggers, indexes, and required columns. Finder deployment `dpl_8tjDSHhUZ2cfvrJtQM61yAXAtNZa` is aliased at `https://sparkle-finder-dev.vercel.app`. The secured internal telemetry smoke route is bearer-token protected, returns `401` without the token, and passed against the deployed alias with 2 conversations, 4 messages, 2 runs, all checks true, and cleanup residuals at zero.
- June 22, 2026 closeout: Finder privacy policy, terms, signup privacy acknowledgment, and account privacy acknowledgment now disclose Nic-Nac AI assistance and memory, including account, collection, Showcase, Wishlist, request, linked-rep, conversation, telemetry/tool, and saved memory context. Finder deployment `dpl_7Ao34Wu45BvhCmyCeXDWjn6T4NTE` is aliased at `https://sparkle-finder-dev.vercel.app`; live checks confirmed updated sign-up and legal pages. This is the baseline product disclosure; attorney/final policy review and broader marketing/onboarding positioning remain launch-readiness follow-up.
- June 22, 2026 later closeout: Finder Nic-Nac now exposes bounded read-only availability parity tools for Sparkle Suite/Finder discovery turns. The `availability` intent can call the compatibility-named `find_rep_board_availability` tool for public Dance Floor dancer leads plus next-show context and `list_upcoming_live_shows` for public live-show timing. These tools are Finder-safe discovery tools only; Suite Dance Floor and show mutations remain blocked from Finder and redirected to Sparkle Suite. Finder deployment `dpl_Bj2YRSp9evCtTEzmR2iD7hmybUHu` is aliased at `https://sparkle-finder-dev.vercel.app`; Vercel inspect confirmed `READY` and public `/`, `/library`, and `/auth/sign-in` checks returned `200`.
- June 22, 2026 latest: Finder Nic-Nac now exposes explicit owner-scoped save tools for collection, Showcase piece, and profile text/visibility turns. The tools are `save_my_collection_item`, `save_my_showcase_piece`, and `update_my_profile`, alongside the existing read tools. Collection and Showcase saves verify the catalog item before mutation, use the signed-in owner's account id, call the existing app-owned persistence helpers, and return explicit saved/failed tool results so Nic-Nac cannot claim success without persistence. Profile saves preserve unspecified profile fields and keep profile photo uploads in the account UI. Finder deployment `dpl_2ykVW81bq6FhEzoNAyad8nQDDhHP` is aliased at `https://sparkle-finder-dev.vercel.app`; Vercel inspect confirmed `READY`, public `/`, `/library`, and `/auth/sign-in` returned `200`, and the secured telemetry smoke route returned `401` without a bearer token.
- June 22, 2026 lint cleanup: Finder lint is clean again. The React `setState`-inside-effect lint errors in `CustomerShowTime` and `FinderNicNacChatbot` were replaced with a shared hydration-safe `useClientFormattedTime` hook, and the remaining warnings were cleaned or intentionally documented. Finder deployment `dpl_2ysBkLzjEXBUSzas94nmbMpA3i5s` is aliased at `https://sparkle-finder-dev.vercel.app`; Vercel inspect confirmed `READY`, public `/`, `/library`, and `/auth/sign-in` returned `200`, and the secured telemetry smoke route returned `401` without a bearer token.
- July 3, 2026: The homepage collector profile card now tracks `Owned`, `Wishlist`, `Diamonds`, `Unicorns`, and `Found by Sparkle Finder` instead of the weaker `Featured`/`Saved` stats. `Found by Sparkle Finder` is backed by collection acquisition source values (`sparkle_finder_lead` and `nic_nac_request`) on owned items. Migration `20260702235634_collection_acquisition_source.sql` was applied and remotely verified in Supabase, then commit `786df5f` was deployed as `dpl_GX6Dzj8DAM61ERf59JHbFTRwUKcf` and aliased at `https://sparkle-finder-dev.vercel.app`.
- July 3, 2026 later: Supabase migration history was repaired and missing additive repo migrations were applied. The old short migration filename `20260613_sparkle_showcase_social_collections.sql` is now normalized to `20260613000000_sparkle_showcase_social_collections.sql`, and `supabase db push --yes` reports `Remote database is up to date.`
- July 4, 2026 latest: The logged-out public homepage is now a simple account-gated Amethyst landing/sign-in page. Anonymous visitors see `Find the pieces you love.`, `Build your collection with Sparkle Finder.`, `Free or Silver account required.`, `Create free account`, and `Sign in`; the old public feature tour and `Find it, favorite it, show it off.` copy are gone from `/`. Signed-in customers still see the mobile-first app home and preserved Finder flows. Deployment `dpl_94RcwTnZgadnTdPh5TsWXtSNBJ3b` from commit `eda1a6f` is aliased at `https://sparkle-finder-dev.vercel.app`.
- July 4, 2026 follow-up: Signed-in users now have visible sign-out controls. Desktop navigation includes `Sign out`, and the authenticated Account page includes a `Sign out` button for mobile/Me flow. Both sign-out links set `prefetch={false}` so Next.js cannot prefetch the GET sign-out route and clear auth before an intentional click. Deployment `dpl_7HEfccnpkPWKN56vfuaQ4ocpez1w` from commit `63e055d` is aliased at `https://sparkle-finder-dev.vercel.app`.
- July 4, 2026 latest follow-up: The Bling Vault/collection section now follows the same mobile-first app canvas as the signed-in home and Find sections instead of the old wide dashboard layout. Hero Piece, Wishlist, and mosaic content stay in a stacked app flow, collection stats are compact panels, and regression tests block the old `max-w-[112rem]`/`xl:grid-cols` pattern from returning. Deployment `dpl_3Dox4Dp6qvNx173ddcYoGGQi1nmy` from commit `521589b` is aliased at `https://sparkle-finder-dev.vercel.app`.
- August 26, 2026: Sparkle Finder appearance is now controlled from the shared Sparkle Suite Control Center. Finder consumes a versioned public appearance contract and applies semantic tokens in its root layout. Amethyst is persisted and is also the safe fallback. Application commit `4a60590a` is live as Finder deployment `dpl_AJm8zgU8jkXwhpSgmEHAdErJFJvX` on `https://yoursparklefinder.com`; live browser inspection confirmed `data-finder-theme=amethyst` and the Amethyst palette.
- August 26, 2026 later: all customer links that leave Finder now open in protected new tabs. The shared `FinderLink` component covers rep, Dance Floor, show, availability, Showcase, and Nic-Nac lead destinations while keeping local Finder routes same-tab. Commit `15f60885` is live as deployment `dpl_EEA1gzkRrdMwJk35KHnF1DzZat3s` on `https://yoursparklefinder.com`.
- September 2, 2026: Finder now exposes a bounded bearer-protected read-only Nic-Nac usage endpoint for the owner-only Suite Cost & Capacity dashboard. The dedicated token is configured as a Vercel Secret in both products' production/preview environments. Commit `97903157` is live as `dpl_CCyNBUN9XUSW4vCfZ9mSxMkoynv5` on `https://yoursparklefinder.com`; an unauthenticated request returns `401`.
- September 2, 2026 later: Finder uses its own OpenAI project and restricted model-request runtime key. Application tip `91273c87` is live as `dpl_9W8PJNL7xKZ88uKfgcb6omrcirfd` on `https://yoursparklefinder.com`. The cost bridge includes persisted reasoning effort and requested intents for owner-only purpose/model-fit reporting.
