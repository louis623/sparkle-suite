# Workspace UI and private matching Smoke evidence

- Application source: isolated clone C:\Users\louis\AppData\Local\Temp\sparkle-lineup-v52-20260926. No production access, publish, or customer data.
- Browser availability: Used bundled Playwright with an existing isolated headless Chromium binary. No signed-in Chrome profile.
- Browser fixture: C:\Users\louis\AppData\Local\Temp\lineup-workspace-ui-smoke (build.cjs, run.cjs, entry.tsx, app.js, app.css). Actual LiveLineupCard and exported LiveQueueTool components; synthetic fetch and Supabase realtime stubs. The fixture navigation button mounts the actual full tool. It is component interaction proof, not a logged-in application route or provider integration test.
- Browser result: 13/13 passed, zero console/page errors. 1280x1000 desktop; 500x850 resize/mobile cancellation. Full result details in results.json and seven PNGs.
- Flow: fixture Workspace home -> move one order by pointer or arrow -> another same-name order stays ahead; open full tool -> Hold exact second April order -> only that order becomes private Hold.
- Lifecycle: hide releases capture synchronously; pageshow/online stays locked pending fresh read; old pre-hide write receipt confirms saved semantics but cannot unlock after resume. Resize cancels and sends no write.
- Freshness: explicit 500ms remaining server budget and 900ms full request latency remains visibly locked. Production policy source freshness remains 45s (capped by lease), with full RTT deducted in the monotonic client deadline.
- Private chips: deferred query completion cannot change chip content/row geometry during pointer drag; pointer-down locks actual row height as a fallback. Audience edit invalidation rejects older in-flight result before cache population. Markup-like preferences render as escaped text.
- Recovery: start-show and archive-recovery deliberate offline receipts each show “Saved. Waiting for the connection to resume.” exactly once and do not auto-repeat. Existing connection reads/revocation remain reachable in the full tool; private-key creation is absent and cannot execute in recovery-only mode. The official assigned-code setup remains intact. The mounted compatibility test verifies recovery with a legacy offline lineup and in read-only mode.
- SQL: one bounded tenant-scoped snapshot computes full-dataset name match count and birthday M/D + four jewelry fields. No page scan, no first-only fallback, duplicate suppression beyond 1,000 rows, tenant isolation, deterministic Unicode 15 full casefold, no audience fields in public DTO. Database INSERT/UPDATE/DELETE trigger advances tenant version. UI binds displayed and pending matches to tenant/show/order/source identity and audience result version.
- Private endpoint: authenticated tenant only; request cannot choose tenant or names; source identities checked before and after query; stale show/identity rejects. 2.5s independent query deadline and 3s client timeout. Missing schema, timeout, query failure, or incomplete receipt hides chips.
- Database migration: supabase/migrations/20260926000200_ss_live_lineup_audience_matches.sql. Applied only to local PGlite tests by this agent; no hosted DB migration.
- Remaining integration proof: actual signed-in Smoke URL with deployed DB/realtime, local extension-to-server transport, and multi-session PostgreSQL concurrency are owned by the main integration pass. Do not interpret these component screenshots as production verification.

- Final scoped checks: 81/81 tests across seven Workspace/audience/compatibility/regression files passed. The final assigned-code update passed all Dashboard and compatibility tests; separate unrelated branding Look-selector SSR expectation failed and was reported to integration. The current 13-case browser run was rebuilt after the final recovery-only and debounced invalidation changes.
